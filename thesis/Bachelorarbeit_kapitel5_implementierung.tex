\chapter{Implementierung}\label{implementierung}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Überblick und
Implementierungsstrategie}\label{uxfcberblick-und-implementierungsstrategie}

Dieses Kapitel dokumentiert die konkrete Umsetzung des in Kapitel 4
konzipierten Systems. Im Fokus stehen die technischen Entscheidungen auf
Code-Ebene: die interne Struktur des FastAPI-Backends mit seinen
Schichten aus Modellen, Repositories, Services und Routern; die
KI-Generierungspipeline von der Ereigniserkennung bis zum
Ticker-Eintrag; die Frontend-Implementierung mit React,
Context-basiertem Zustandsmanagement und dem slash-command-getriebenen
Eingabesystem; sowie die n8n-Workflow-Implementierung der zentralen
Automatisierungsworkflows. Ergänzend werden die TypeScript-Migration und
die Testabdeckung als Qualitätsmerkmale des Entwicklungsprozesses
dokumentiert.

Die Implementierung folgt dem Grundsatz \textbf{„so einfach wie nötig,
so modular wie sinnvoll''}: Standardpfade (CRUD, Listabfragen) sind
bewusst direkt gehalten (Router → Repository), während die
LLM-Generierung mit ihren Retry-, Semaphore- und Few-Shot-Mechanismen in
einen dedizierten Service extrahiert ist.

Die n8n-Workflows (Abschnitt 5.5) werden abweichend von der
konzeptionellen Reihenfolge in Kapitel 4 nach der
Frontend-Implementierung beschrieben: Die Workflows bauen auf
Backend-Endpunkten auf, die bis dahin vollständig dokumentiert sind,
sodass die Beschreibung der Orchestrierungslogik erst an dieser Stelle
vollständig nachvollziehbar ist.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Backend-Implementierung}\label{backend-implementierung}

\subsection{Projektstruktur und Application Entry
Point}\label{projektstruktur-und-application-entry-point}

Das Backend ist unterhalb von \texttt{backend/app/} in sieben
funktionale Pakete gegliedert:

\begin{verbatim}
backend/app/
├── api/v1/          # HTTP-Router (FastAPI)
├── models/          # SQLAlchemy ORM-Modelle (17 Dateien)
├── repositories/    # Datenbankzugriff (je Entität eine Klasse)
├── schemas/         # Pydantic-Schemas (Request / Response)
├── services/        # Fachliche Services (llm_service, ticker_service)
├── utils/           # Hilfsmodule (llm_context_builders, evaluation_metrics)
└── core/            # Konfiguration, Datenbankverbindung, Enums, Konstanten
\end{verbatim}

Diese Aufteilung entspricht dem klassischen
Repository-Service-Router-Muster: Router delegieren an Repositories für
einfache CRUD-Operationen und an Services für Logik, die mehrere
Repositories oder externe Aufrufe koordiniert.

\texttt{app/main.py} ist der einzige Einstiegspunkt. Er registriert alle
12 Router unter dem Pfad-Prefix \texttt{/api/v1}, konfiguriert
CORS-Middleware und bindet statische Dateien (Thumbnail-Cache) ein. Der
WebSocket-Endpunkt \texttt{/ws/media} wird ohne API-Prefix eingehängt,
da er kein REST-Ressourcenpfad ist. Beim Start führt
\texttt{Base.metadata.create\_all(bind=engine)} ein idempotentes
Schema-Bootstrapping durch, das bei erstmaliger Inbetriebnahme alle
Tabellen anlegt; Schema-Änderungen im Produktionsbetrieb werden via
Alembic-Migrationen gesteuert.

Die Anwendung stellt zwei Metaendpunkte bereit:

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}ll@{}}
\toprule\noalign{}
Endpunkt & Funktion \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{GET\ /} & Statusantwort mit Version (\texttt{0.3.0}) \\
\texttt{GET\ /health} & Datenbankverbindungscheck
(\texttt{healthy}/\texttt{degraded}) \\
\end{longtable}
}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Datenmodelle: Datenbankschema und
API-Validierung}\label{datenmodelle-datenbankschema-und-api-validierung}

\textbf{SQLAlchemy-Datenbankmodelle} --- Das Schema umfasst \textbf{17
ORM-Modelle} (18 Datenbanktabellen, davon eine via Migration). Die
zentralen Domänenobjekte sind:

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.1848}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.2065}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.6087}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Modell
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Tabelle
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Kernfelder
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{Country} & \texttt{countries} & \texttt{id}, \texttt{name},
\texttt{uid} \\
\texttt{Team} & \texttt{teams} & \texttt{id}, \texttt{uid},
\texttt{name}, \texttt{external\_id}, \texttt{is\_partner\_team} \\
\texttt{Competition} & \texttt{competitions} & \texttt{id},
\texttt{title}, \texttt{season\_id} \\
\texttt{Match} & \texttt{matches} & \texttt{id}, \texttt{uid},
\texttt{match\_state}, \texttt{match\_phase}, \texttt{ticker\_mode} \\
\texttt{Event} & \texttt{events} & \texttt{id}, \texttt{match\_id},
\texttt{event\_type}, \texttt{time}, \texttt{description} \\
\texttt{SyntheticEvent} & \texttt{synthetic\_events} & \texttt{id},
\texttt{match\_id}, \texttt{type}, \texttt{data} (JSONB) \\
\texttt{TickerEntry} & \texttt{ticker\_entries} & → siehe unten \\
\texttt{Lineup} & \texttt{lineups} & \texttt{player\_id},
\texttt{status}, \texttt{formation\_place}, \texttt{position} \\
\texttt{MatchStatistic} & \texttt{match\_statistics} & 19 typisierte
Spalten (Pässe, Zweikämpfe, Schüsse\ldots) \\
\texttt{PlayerStatistic} & \texttt{player\_statistics} &
\texttt{player\_id}, \texttt{rating}, \texttt{goals}, \texttt{assists},
\ldots{} \\
\texttt{StyleReference} & \texttt{style\_references} &
\texttt{event\_type}, \texttt{instance}, \texttt{league}, \texttt{text}
(Few-Shot) \\
\texttt{MediaQueue} & \texttt{media\_queue} & \texttt{media\_id},
\texttt{thumbnail\_url}, \texttt{event\_id}, \texttt{status} \\
\texttt{MediaClip} & \texttt{media\_clips} & \texttt{source},
\texttt{vid}, \texttt{thumbnail\_url}, \texttt{match\_id} \\
\end{longtable}
}

Das \texttt{TickerEntry}-Modell hat besondere Bedeutung im Lifecycle des
Systems: Es verknüpft über \texttt{event\_id} (FK auf \texttt{events})
und \texttt{synthetic\_event\_id} (FK auf \texttt{synthetic\_events})
den generierten Text mit seinem Auslöser. Die Felder \texttt{status}
(\texttt{draft}/\texttt{published}/\texttt{rejected}) und
\texttt{source} (\texttt{ai}/\texttt{manual}) bilden den
Ticker-Lifecycle ab (vgl. Kap. 4.3.2). Drei zusammengesetzte Indizes
(\texttt{ix\_ticker\_match\_status}, \texttt{ix\_ticker\_match\_phase},
\texttt{ix\_ticker\_event\_id}) optimieren die häufigsten Abfragen:
publizierte Einträge je Spiel, Phase-Filter und den
Deduplizierungs-Check per Event. Das Status-Enum wird als nicht-nativ
gespeichert (\texttt{native\_enum=False}), damit Migrationen ohne
Enum-Typ-Änderungen in PostgreSQL auskommen.

\textbf{Pydantic-Schemas und API-Validierung} --- Für jeden Router gibt
es eigenständige Pydantic-Schemas für Create-, Update- und
Response-Strukturen. Alle Schemas nutzen
\texttt{alias\_generator=to\_camel} aus
\texttt{pydantic.alias\_generators}, damit JSON-Payloads in camelCase
kommuniziert werden, während Python-intern snake\_case gilt.

Ein spezifisches Beispiel illustriert die Alias-Mechanik für
Match-Scores:

\begin{Shaded}
\begin{Highlighting}[]
\KeywordTok{class}\NormalTok{ MatchResponse(BaseModel):}
\NormalTok{    home\_score: Optional[}\BuiltInTok{int}\NormalTok{] }\OperatorTok{=}\NormalTok{ Field(}\VariableTok{None}\NormalTok{, serialization\_alias}\OperatorTok{=}\StringTok{"teamHomeScore"}\NormalTok{)}
\NormalTok{    away\_score: Optional[}\BuiltInTok{int}\NormalTok{] }\OperatorTok{=}\NormalTok{ Field(}\VariableTok{None}\NormalTok{, serialization\_alias}\OperatorTok{=}\StringTok{"teamAwayScore"}\NormalTok{)}
\end{Highlighting}
\end{Shaded}

Hier wird \texttt{serialization\_alias} (nicht \texttt{alias})
verwendet, da das Frontend die Partner-API-Feldnamen erwartet
(\texttt{teamHomeScore}/\texttt{teamAwayScore}), während
Schreiboperationen über \texttt{populate\_by\_name=True} auch den
Python-Namen akzeptieren.

Diese duale Alias-Konvention zieht sich durch alle Schemas: Felder, die
Spielstandwerte aus der Partner-API widerspiegeln, erhalten explizite
\texttt{serialization\_alias}-Werte mit Partner-Konvention
(\texttt{teamHomeScore}). Standard-Beziehungsfelder hingegen erhalten
durch den globalen \texttt{alias\_generator=to\_camel} automatisch
kanonisches camelCase (\texttt{homeTeamId}, \texttt{awayTeamId}). Die
Koexistenz beider Konventionen in derselben Schema-Klasse ist eine
bewusste Designentscheidung: Das Frontend konsumiert sowohl
Partner-API-Daten als auch eigenständig modellierte Beziehungen, und
beide müssen ohne Feldumbenennungen im Frontend konsistent adressierbar
sein.

Für den Match-Status-Lifecycle definiert \texttt{MatchUpdate} ein
explizites Alias:

\begin{Shaded}
\begin{Highlighting}[]
\NormalTok{match\_state: Optional[MatchState] }\OperatorTok{=}\NormalTok{ Field(}\VariableTok{None}\NormalTok{, alias}\OperatorTok{=}\StringTok{"state"}\NormalTok{)}
\end{Highlighting}
\end{Shaded}

Für Ticker-Einträge wird der Status über ein separates PATCH-Schema
gesteuert, um versehentliche Massenänderungen durch offene
Update-Schemas zu vermeiden.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Repository-Schicht}\label{repository-schicht}

Jede Entität hat ein dediziertes Repository, das alle SQL-Operationen
kapselt. Alle Repositories erben von einer gemeinsamen
\texttt{BaseRepository{[}T{]}}-Basisklasse, die die generische Operation
\texttt{exists()} bereitstellt; jede Instanz erhält eine
\texttt{Session} im Konstruktor:

\begin{Shaded}
\begin{Highlighting}[]
\KeywordTok{class}\NormalTok{ TickerEntryRepository(BaseRepository[TickerEntry]):}
\NormalTok{    model }\OperatorTok{=}\NormalTok{ TickerEntry}

    \KeywordTok{def}\NormalTok{ get\_by\_event(}\VariableTok{self}\NormalTok{, event\_id: }\BuiltInTok{int}\NormalTok{) }\OperatorTok{{-}\textgreater{}}\NormalTok{ Optional[TickerEntry]:}
        \ControlFlowTok{return} \VariableTok{self}\NormalTok{.db.query(TickerEntry).}\BuiltInTok{filter}\NormalTok{(}
\NormalTok{            TickerEntry.event\_id }\OperatorTok{==}\NormalTok{ event\_id}
\NormalTok{        ).first()}

    \KeywordTok{def}\NormalTok{ get\_by\_match(}\VariableTok{self}\NormalTok{, match\_id: }\BuiltInTok{int}\NormalTok{, status}\OperatorTok{=}\VariableTok{None}\NormalTok{) }\OperatorTok{{-}\textgreater{}} \BuiltInTok{list}\NormalTok{[TickerEntry]:}
\NormalTok{        ...}
\end{Highlighting}
\end{Shaded}

Diese Kapselung hat zwei Vorteile: Router bleiben frei von ORM-Details,
und Repository-Methoden können direkt mit echten Datenbanken in
Integrationstests geprüft werden --- ohne Mock-Schichten, die
Implementierungsabweichungen verbergen.

Das \texttt{MatchRepository} definiert eine interne
\texttt{\_base\_query()}-Methode, die Heimteam, Auswärtsteam, Wettbewerb
und Saison per \texttt{joinedload} in einer einzigen Datenbankabfrage
vorlädt. \texttt{get\_by\_id()} nutzt diese Methode konsequent:

\begin{Shaded}
\begin{Highlighting}[]
\KeywordTok{class}\NormalTok{ MatchRepository(BaseRepository[Match]):}
\NormalTok{    model }\OperatorTok{=}\NormalTok{ Match}

    \KeywordTok{def}\NormalTok{ \_base\_query(}\VariableTok{self}\NormalTok{) }\OperatorTok{{-}\textgreater{}}\NormalTok{ Query:}
        \ControlFlowTok{return} \VariableTok{self}\NormalTok{.db.query(Match).options(}
\NormalTok{            joinedload(Match.home\_team),}
\NormalTok{            joinedload(Match.away\_team),}
\NormalTok{            joinedload(Match.competition),}
\NormalTok{            joinedload(Match.season),}
\NormalTok{        )}

    \KeywordTok{def}\NormalTok{ get\_by\_id(}\VariableTok{self}\NormalTok{, match\_id: }\BuiltInTok{int}\NormalTok{) }\OperatorTok{{-}\textgreater{}}\NormalTok{ Optional[Match]:}
        \ControlFlowTok{return} \VariableTok{self}\NormalTok{.\_base\_query().}\BuiltInTok{filter}\NormalTok{(Match.}\BuiltInTok{id} \OperatorTok{==}\NormalTok{ match\_id).first()}
\end{Highlighting}
\end{Shaded}

Diese Methode wird von der KI-Generierungsroute intensiv genutzt, da für
jeden Aufruf vollständige Kontext-Daten (Teamnamen, Wettbewerb) benötigt
werden.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{API-Router:
Endpunktstruktur}\label{api-router-endpunktstruktur}

Der Ticker-Bereich ist in drei Router aufgeteilt, die gemeinsam unter
\texttt{/api/v1/ticker} eingehängt werden:

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.1753}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.2062}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.6186}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Router
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Datei
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Kernoperationen
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{ticker\_crud} & \texttt{ticker\_crud.py} & GET list, GET single,
PATCH status, DELETE, POST manual \\
\texttt{ticker\_generate} & \texttt{ticker\_generate.py} & POST
generate/\{event\_id\}, generate-synthetic, generate-batch \\
\texttt{ticker\_batch} & \texttt{ticker\_batch.py} & POST
translate-batch, generate-match-phases \\
\end{longtable}
}

Diese Aufteilung verhindert, dass eine einzelne Routerdatei durch die
Kombination aus CRUD-, Generierungs- und Batch-Endpunkten
unübersichtlich wird.

Ein kapselndes Hilfsmuster zieht sich durch alle Router:
\texttt{require\_or\_404(value,\ message)} --- eine schmale
Utility-Funktion in \texttt{utils/http\_errors.py}, die
\texttt{None}-Rückgaben aus Repository-Aufrufen direkt in eine
\texttt{HTTPException(404)} überführt. Das eliminiert das repetitive
\texttt{if\ obj\ is\ None:\ raise\ HTTPException(404,\ ...)} in Routern
und stellt sicher, dass alle 404-Antworten einheitlich formuliert sind.
Der Batch-Endpunkt
\texttt{POST\ /api/v1/ticker/translate-batch/\{match\_id\}} ruft für
alle publizierten KI-Einträge eines Spiels den LLM-Service mit
reduzierter Temperatur (\texttt{0.1}) auf, um sprachlich
deterministische Übersetzungen zu erzeugen --- eine gegenüber der
Originalgenerierung bewusst andere Parametrisierung.

Die wichtigsten Endpunkte gliedern sich in vier funktionale Bereiche:
\textbf{Stammdaten} (Countries, Teams, Matches, Seasons, Competitions),
\textbf{Ticker-Lifecycle} (CRUD, Generierung per Event,
Batch-Generierung für Synthetic Events, Übersetzung), \textbf{Medien \&
Clips} (ScorePlay-Import, Media-Queue, WebSocket) und
\textbf{Spielkontext} (Lineups, Statistiken, Events). Insgesamt umfasst
die API über 70 Routen; die vollständige Endpunktliste ist über die
automatisch generierte OpenAPI-Dokumentation unter \texttt{/api/docs}
einsehbar.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Ticker-Service:
Domänenlogik}\label{ticker-service-domuxe4nenlogik}

Der \texttt{ticker\_service} kapselt die Domänenlogik für KI-Einträge.
Er koordiniert Datenbankabfragen, Kontextaufbau und den LLM-Aufruf, hält
dabei aber die Schichten klar getrennt: \texttt{llm\_service.py} hat
keine Datenbankabhängigkeit.

Die vier zentralen Funktionen:

\textbf{\texttt{score\_at\_event()}} berechnet den Spielstand zum
Zeitpunkt eines Torereignisses. Da die Football-API den kumulativen
Stand liefert, muss der Stand \emph{vor} dem aktuellen Event
rekonstruiert werden. Die Funktion lädt alle Tore bis zur
\texttt{position} des Events, unterscheidet zwischen regulären Toren und
Eigentoren (letztere zählen für den Gegner) und akkumuliert die Scores
für Heim- und Auswärtsteam.

\textbf{\texttt{build\_match\_context()}} erstellt das
Kontext-Dictionary für den LLM-Prompt: Teamnamen, aktueller Stand,
Matchzustand, Spielminute und Liga.

\textbf{\texttt{call\_llm()}} ist der zentrale, Semaphore-gesicherte
LLM-Aufruf. Er lädt zunächst aus dem \texttt{StyleReferenceRepository}
bis zu drei Stilbeispiele für
\texttt{event\_type\ +\ instance\ +\ league} als Few-Shot-Kontext und
delegiert dann an \texttt{generate\_ticker\_text()}:

\begin{Shaded}
\begin{Highlighting}[]
\ControlFlowTok{async} \ControlFlowTok{with}\NormalTok{ \_llm\_semaphore:   }\CommentTok{\# asyncio.Semaphore(settings.LLM\_CONCURRENCY)}
    \ControlFlowTok{return} \ControlFlowTok{await}\NormalTok{ generate\_ticker\_text(}
\NormalTok{        event\_type}\OperatorTok{=}\NormalTok{event\_type,}
\NormalTok{        style\_references}\OperatorTok{=}\NormalTok{style\_references,  }\CommentTok{\# aus DB geladen}
\NormalTok{        ...}
\NormalTok{    )}
\end{Highlighting}
\end{Shaded}

Die Semaphore ist auf Modulebene als Singleton definiert
(\texttt{\_llm\_semaphore\ =\ asyncio.Semaphore(settings.LLM\_CONCURRENCY)},
Standard: 8) und begrenzt gleichzeitige LLM-Aufrufe pro Prozessinstanz.

\textbf{\texttt{make\_ai\_entry()}} ist ein schlanker Builder, der aus
den LLM-Ergebnissen ein \texttt{TickerEntryCreate}-Schema erzeugt. Er
setzt \texttt{source="ai"} und legt den initialen Status abhängig vom
Aufrufkontext fest (\texttt{draft} im Co-op-Modus, \texttt{published} im
Auto-Modus).

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{LLM-Service: Prompt-Aufbau und
Provider-Dispatch}\label{llm-service-prompt-aufbau-und-provider-dispatch}

Der \texttt{LLMService} ist eine single-class-Implementierung, die beim
Initialisieren den passenden Client instanziiert und über ein
Dispatch-Dictionary aufruft:

\begin{Shaded}
\begin{Highlighting}[]
\NormalTok{dispatch }\OperatorTok{=}\NormalTok{ \{}
    \StringTok{"mock"}\NormalTok{:       }\VariableTok{self}\NormalTok{.\_generate\_mock\_text,}
    \StringTok{"gemini"}\NormalTok{:     }\VariableTok{self}\NormalTok{.\_generate\_gemini\_text,}
    \StringTok{"openrouter"}\NormalTok{: }\VariableTok{self}\NormalTok{.\_generate\_openrouter\_text,}
    \StringTok{"openai"}\NormalTok{:     }\VariableTok{self}\NormalTok{.\_generate\_openai\_text,}
    \StringTok{"anthropic"}\NormalTok{:  }\VariableTok{self}\NormalTok{.\_generate\_anthropic\_text,}
\NormalTok{\}}
\ControlFlowTok{return}\NormalTok{ dispatch[}\VariableTok{self}\NormalTok{.provider](}\OperatorTok{**}\NormalTok{kwargs)}
\end{Highlighting}
\end{Shaded}

Das Dispatch-Pattern macht es trivial, neue Provider hinzuzufügen:
Implementierung einer Methode \texttt{\_generate\_X\_text()} und ein
Eintrag im Dictionary.

Der Prompt wird in vier modularen Methoden aufgebaut:

\textbf{\texttt{\_build\_event\_lines()}} erzeugt die Fakten-Sektion.
Jeder Parameter (Ereignistyp, Detail, Minute, Spieler, Team) wird nur
dann eingebunden, wenn er vorhanden ist. Die Ereignisbezeichnung wird
aus \texttt{EVENT\_TYPE\_LABEL} in lesbares Deutsch übersetzt (z. B.
\texttt{"goal"} → \texttt{"Tor"}).

\textbf{\texttt{\_build\_few\_shot\_block()}} formatiert die
Stilreferenzen als nummerierte Beispiele:

\begin{verbatim}
### STILREFERENZEN
Schreibe in exakt diesem Stil (Rhythmus, Wortwahl, Emotionalität):
- "Mustermann trifft mit einem satten Rechtsschuss – 1:0!"
- "Eintracht schlägt zu – der erste Treffer des Abends!"
\end{verbatim}

\textbf{\texttt{\_build\_prematch\_parts()}} ergänzt für Pre-Match-Typen
eine zusätzliche harte Instruktion: Das Modell darf keine Live-Szenen
erfinden, sondern ausschließlich Vorschau- und Analyseinhalte
produzieren.

Der vollständig zusammengesetzte System-Prompt folgt der in Abschnitt
4.5.2 beschriebenen modularen Struktur aus sechs Bausteinen
(Rolleninstruktion, Stilinstruktion, Faktenblock, Match-Kontext,
Few-Shot-Block, Regelblock). Für Pre-Match-Typen fügt
\texttt{\_build\_prematch\_parts()} eine zusätzliche harte Schutzregel
ein, die das Modell auf Vorschau- und Analyseinhalte beschränkt. Die
vier \texttt{\_build\_*}-Methoden erzeugen die Bausteine unabhängig
voneinander, sodass fehlende Informationen (z. B. kein Spieler bei
Phasenereignissen) den Prompt nicht invalidieren, sondern den
entsprechenden Block auslassen.

LLM-Parameter: \texttt{temperature=0.3} für konsistente Ausgaben,
\texttt{max\_tokens} aus \texttt{LLM\_MAX\_TOKENS} (konfigurierbar). Bei
Rate-Limit-Fehlern werden bis zu \texttt{LLM\_RETRY\_ATTEMPTS} (3)
Versuche mit linearem Backoff (\texttt{30\ s\ /\ 60\ s\ /\ 90\ s})
unternommen; bei sonstigen transienten Fehlern greift exponentielles
Backoff (\texttt{1\ s\ /\ 2\ s\ /\ 4\ s}).

\textbf{Normalisierung des Event-Typs} erfolgt vorab in
\texttt{\_normalize\_event\_type()}. Die \texttt{EVENT\_TYPE\_MAP}
übersetzt externe Strings (Partner-API: \texttt{PartnerGoal},
Football-API: \texttt{Goal}) auf interne Bezeichner (\texttt{goal}).
Nicht erkannte Typen fallen auf \texttt{"comment"} zurück.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{WebSocket-Endpunkt für
Media-Queue}\label{websocket-endpunkt-fuxfcr-media-queue}

Der WebSocket-Endpunkt \texttt{/ws/media} wird durch einen dedizierten
\texttt{MediaConnectionManager} verwaltet:

\begin{Shaded}
\begin{Highlighting}[]
\KeywordTok{class}\NormalTok{ MediaConnectionManager:}
    \KeywordTok{def} \FunctionTok{\_\_init\_\_}\NormalTok{(}\VariableTok{self}\NormalTok{) }\OperatorTok{{-}\textgreater{}} \VariableTok{None}\NormalTok{:}
        \VariableTok{self}\NormalTok{.active\_connections: }\BuiltInTok{list}\NormalTok{[WebSocket] }\OperatorTok{=}\NormalTok{ []}

    \ControlFlowTok{async} \KeywordTok{def} \ExtensionTok{connect}\NormalTok{(}\VariableTok{self}\NormalTok{, websocket: WebSocket) }\OperatorTok{{-}\textgreater{}} \VariableTok{None}\NormalTok{:}
        \ControlFlowTok{await}\NormalTok{ websocket.accept()}
        \VariableTok{self}\NormalTok{.active\_connections.append(websocket)}

    \ControlFlowTok{async} \KeywordTok{def}\NormalTok{ broadcast(}\VariableTok{self}\NormalTok{, data: }\BuiltInTok{dict}\NormalTok{) }\OperatorTok{{-}\textgreater{}} \VariableTok{None}\NormalTok{:}
\NormalTok{        dead: }\BuiltInTok{list}\NormalTok{[WebSocket] }\OperatorTok{=}\NormalTok{ []}
        \ControlFlowTok{for}\NormalTok{ ws }\KeywordTok{in} \VariableTok{self}\NormalTok{.active\_connections:}
            \ControlFlowTok{try}\NormalTok{:}
                \ControlFlowTok{await}\NormalTok{ ws.send\_json(data)}
            \ControlFlowTok{except}\NormalTok{ (WebSocketDisconnect, }\PreprocessorTok{RuntimeError}\NormalTok{):}
\NormalTok{                dead.append(ws)}
        \ControlFlowTok{for}\NormalTok{ ws }\KeywordTok{in}\NormalTok{ dead:}
            \VariableTok{self}\NormalTok{.disconnect(ws)}
\end{Highlighting}
\end{Shaded}

Wenn das Backend neue Media-Queue-Einträge über
\texttt{POST\ /api/v1/media/incoming} erhält, ruft der Router
anschließend
\texttt{manager.broadcast(\{"type":\ "new\_media",\ "items":\ {[}...{]}\})}
auf. Alle verbundenen Clients erhalten das Update in Echtzeit. Der
Manager hält Verbindungen in einer einfachen In-Memory-Liste, was für
die aktuelle Nutzung (wenige gleichzeitige Redakteur-Clients)
ausreichend ist; bei Multi-Prozess-Deployments wäre ein externes
Pub/Sub-System (z. B. Redis) erforderlich.

Die persistente Seite der Media-Queue wird durch das
\texttt{MediaQueueStatus}-Enum gesteuert, das zwei Zustände
unterscheidet: \texttt{pending} (Medien sind eingegangen und warten auf
redaktionelle Zuordnung) und \texttt{published} (Medien wurden einem
Ticker-Eintrag zugewiesen). Das Frontend zeigt im
\texttt{MediaPickerPanel} ausschließlich \texttt{pending}-Einträge an;
sobald ein Redakteur ein Bild einem Ticker-Eintrag anhängt, wird der
Status per \texttt{PATCH\ /api/v1/media/queue/\{id\}} auf
\texttt{published} gesetzt. Dieses einfache Zwei-Zustands-Modell
verhindert, dass einmal zugeordnete Medien in der Auswahl erneut
auftauchen. Der korrespondierende Frontend-Hook
\texttt{useMediaWebSocket}, der die Verbindungslebensdauer, das
exponentielle Reconnect-Backoff und das Nachrichten-Handling auf
Client-Seite übernimmt, ist in Abschnitt 5.4.5 beschrieben.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{KI-Generierungspipeline}\label{ki-generierungspipeline}

\subsection{Ablaufdiagramm}\label{ablaufdiagramm}

Die folgende Darstellung zeigt den vollständigen Ablauf von einem neuen
Live-Ereignis bis zum publizierten Ticker-Eintrag:

\begin{Shaded}
\begin{Highlighting}[]
\NormalTok{sequenceDiagram}
\NormalTok{    participant n8n as n8n Workflow}
\NormalTok{    participant API as FastAPI Backend}
\NormalTok{    participant DB as PostgreSQL}
\NormalTok{    participant LLM as LLM Provider}
\NormalTok{    participant FE as React Frontend}

\NormalTok{    n8n{-}\textgreater{}\textgreater{}API: POST /api/v1/events (Ereignis{-}Import)}
\NormalTok{    API{-}\textgreater{}\textgreater{}DB: INSERT INTO events}
\NormalTok{    n8n{-}\textgreater{}\textgreater{}API: POST /api/v1/ticker/generate/\{event\_id\}}
\NormalTok{    API{-}\textgreater{}\textgreater{}DB: SELECT ticker\_entry WHERE event\_id (Dedup)}
\NormalTok{    alt Eintrag existiert bereits}
\NormalTok{        API{-}{-}\textgreater{}\textgreater{}n8n: 200 OK (existierender Eintrag)}
\NormalTok{    else Neues Ereignis}
\NormalTok{        API{-}\textgreater{}\textgreater{}DB: SELECT match + teams (joinedload)}
\NormalTok{        API{-}\textgreater{}\textgreater{}DB: SELECT style\_references (Few{-}Shot)}
\NormalTok{        API{-}\textgreater{}\textgreater{}LLM: Prompt (Fakten + Kontext + Stilreferenzen)}
\NormalTok{        LLM{-}{-}\textgreater{}\textgreater{}API: Generierter Text + Modellname}
\NormalTok{        API{-}\textgreater{}\textgreater{}DB: INSERT ticker\_entry (status=draft|published)}
\NormalTok{        API{-}{-}\textgreater{}\textgreater{}n8n: 201 Created}
\NormalTok{    end}
\NormalTok{    FE{-}\textgreater{}\textgreater{}API: GET /api/v1/ticker/\{match\_id\} (Polling 5s)}
\NormalTok{    API{-}{-}\textgreater{}\textgreater{}FE: TickerEntry[] (inkl. neuer Entwurf)}
\NormalTok{    alt Modus=coop}
\NormalTok{        FE{-}{-}\textgreater{}\textgreater{}FE: Entwurf in Draft{-}Queue anzeigen}
\NormalTok{        note over FE: Redakteur: TAB (accept) / ESC (reject)}
\NormalTok{        FE{-}\textgreater{}\textgreater{}API: PATCH /api/v1/ticker/\{id\} \{status:"published"\}}
\NormalTok{    else Modus=auto}
\NormalTok{        note over DB: status=published gesetzt bei Generierung}
\NormalTok{    end}
\end{Highlighting}
\end{Shaded}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Deduplizierung}\label{deduplizierung}

Ein zentrales Qualitätsmerkmal der Pipeline ist die Deduplizierung: Der
Generierungsendpunkt prüft vor jedem LLM-Aufruf, ob bereits ein
Ticker-Eintrag für die \texttt{event\_id} existiert:

\begin{Shaded}
\begin{Highlighting}[]
\NormalTok{existing }\OperatorTok{=}\NormalTok{ ticker\_repo.get\_by\_event(event\_id)}
\ControlFlowTok{if}\NormalTok{ existing:}
    \ControlFlowTok{return}\NormalTok{ existing  }\CommentTok{\# Idempotenter Rückgabepfad ohne LLM{-}Aufruf}
\end{Highlighting}
\end{Shaded}

Dies macht den Endpunkt idempotent gegenüber Mehrfachaufrufen --- ein
wichtiges Merkmal, da n8n-Workflows bei Fehlern automatisch wiederholen.
Ohne diese Prüfung würde jeder Retry einen zusätzlichen Ticker-Eintrag
mit unterschiedlichem (nicht-deterministischem) Text erzeugen.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Statusentscheidung und
Modus-Steuerung}\label{statusentscheidung-und-modus-steuerung}

Der initiale Status des erzeugten Ticker-Eintrags wird über den
Aufrufparameter \texttt{auto\_publish} gesteuert, den n8n aus dem
\texttt{ticker\_mode} des Spiels ableitet (vgl. Kap. 4.3.3 für die
konzeptionelle Beschreibung der drei Betriebsmodi):

\begin{Shaded}
\begin{Highlighting}[]
\NormalTok{status}\OperatorTok{=}\NormalTok{TickerStatus.published }\ControlFlowTok{if}\NormalTok{ data.auto\_publish }\ControlFlowTok{else}\NormalTok{ TickerStatus.draft}
\end{Highlighting}
\end{Shaded}

Damit wird die Modus-Logik zu einem reinen Aufrufparameter des Backends
--- die Entscheidungsinstanz ist n8n auf Basis des in der Datenbank
gespeicherten \texttt{ticker\_mode}. Das Backend selbst ist
modus-agnostisch: Es erhält einen Booleschen Wert und setzt den Status
entsprechend.

Die drei Modi übersetzen sich auf Implementierungsebene in drei
verschiedene Pfade:

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.1270}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2222}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.1746}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.4762}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Modus
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
\texttt{auto\_publish}
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Ergebnis
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Redakteurs-Eingriff
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{auto} & \texttt{True} & \texttt{published} & --- \\
\texttt{coop} & \texttt{False} & \texttt{draft} & TAB (accept) / ESC
(reject) \\
\texttt{manual} & --- & --- & Slash-Command → \texttt{POST\ /manual} \\
\end{longtable}
}

Im \texttt{manual}-Modus wird der Generierungs-Endpunkt gar nicht
aufgerufen: n8n importiert zwar weiterhin Ereignisse, triggert aber
keinen LLM-Aufruf. Ticker-Einträge entstehen ausschließlich über den
\texttt{POST\ /api/v1/ticker/manual}-Endpunkt, den das Frontend aus dem
Slash-Command-Parser heraus bedient. Der Eintrag wird dort direkt mit
\texttt{status=published} angelegt, da die Redakteurin den Text selbst
verfasst hat und keine weitere Freigabe erforderlich ist.

Im \texttt{coop}-Modus liegt die gesamte UX-Last auf der Draft-Queue:
Das Frontend zeigt neue \texttt{draft}-Einträge mit
\texttt{TAB}/\texttt{ESC}-Shortcuts an (vgl. Abschnitt 5.4.6), und die
Entscheidungs-Latenz beträgt je nach Komplexität des Entwurfs 15--30
Sekunden. Dieses Design stellt sicher, dass keine KI-generierten Texte
ohne menschliche Prüfung publiziert werden --- eine Entscheidung, die
direkt aus dem journalistischen Qualitätsanspruch in Kapitel 2.1 folgt.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Zustandsautomat der
Ticker-Einträge}\label{zustandsautomat-der-ticker-eintruxe4ge}

Das konzeptionelle Statusmodell (\texttt{draft} → \texttt{published} /
\texttt{rejected}, Undo-Übergang \texttt{published\ →\ draft}) ist
vollständig in Abschnitt 4.3.2 beschrieben. Implementierungsseitig sind
zwei Details hervorzuheben:

Der Übergang \texttt{published\ →\ draft} (Undo) ist im Frontend als
\textbf{Toast-Aktion} realisiert: Nach jeder Publikation erscheint für 5
Sekunden ein Widerruf-Button. Über \texttt{PATCH\ /api/v1/ticker/\{id\}}
mit \texttt{\{\ "status":\ "draft"\ \}} wird der Eintrag zurückgesetzt
und steht erneut zur Bearbeitung bereit.

\texttt{rejected}-Einträge werden nicht aus der Datenbank gelöscht,
sondern behalten ihren Status. Diese Entscheidung sichert die
Reproduzierbarkeit der Evaluation: Alle je generierten Einträge --- auch
verworfene --- sind für spätere Qualitätsanalysen auswertbar.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Frontend-Implementierung}\label{frontend-implementierung}

\subsection{Komponentenhierarchie}\label{komponentenhierarchie}

Das Frontend ist als React-Anwendung mit TypeScript organisiert. Die
Hauptansicht (\texttt{LiveTicker}) gliedert sich in drei
Panel-Komponenten, die von gemeinsam genutzten Hooks und Contexts
gespeist werden:

\begin{Shaded}
\begin{Highlighting}[]
\NormalTok{graph TD}
\NormalTok{    App {-}{-}\textgreater{} LiveTicker}
\NormalTok{    LiveTicker {-}{-}\textgreater{} |TickerModeContext| LeftPanel}
\NormalTok{    LiveTicker {-}{-}\textgreater{} |TickerModeContext| CenterPanel}
\NormalTok{    LiveTicker {-}{-}\textgreater{} |TickerModeContext| RightPanel}
\NormalTok{    LiveTicker {-}{-}\textgreater{} |TickerDataContext| LeftPanel}
\NormalTok{    LiveTicker {-}{-}\textgreater{} |TickerDataContext| CenterPanel}
\NormalTok{    LiveTicker {-}{-}\textgreater{} |TickerDataContext| RightPanel}
\NormalTok{    LiveTicker {-}{-}\textgreater{} |TickerActionsContext| CenterPanel}

\NormalTok{    CenterPanel {-}{-}\textgreater{} EventCard}
\NormalTok{    CenterPanel {-}{-}\textgreater{} EntryEditor}
\NormalTok{    CenterPanel {-}{-}\textgreater{} AIDraft}
\NormalTok{    CenterPanel {-}{-}\textgreater{} SummarySection}
\NormalTok{    CenterPanel {-}{-}\textgreater{} MediaPickerPanel}
\NormalTok{    CenterPanel {-}{-}\textgreater{} YouTubePanel}
\NormalTok{    CenterPanel {-}{-}\textgreater{} TwitterPanel}
\NormalTok{    CenterPanel {-}{-}\textgreater{} InstagramPanel}

\NormalTok{    LeftPanel {-}{-}\textgreater{} PublishedEntry}
\NormalTok{    RightPanel {-}{-}\textgreater{} Collapsible["Collapsible (Statistiken, Aufstellung, Spieler)"]}
\NormalTok{    RightPanel {-}{-}\textgreater{} FormationColumn}
\NormalTok{    RightPanel {-}{-}\textgreater{} StatRow}

\NormalTok{    EventCard {-}{-}\textgreater{} |onGenerate| TickerActionsContext}
\NormalTok{    AIDraft {-}{-}\textgreater{} |onPublished/onDraftActive| TickerActionsContext}
\NormalTok{    EntryEditor {-}{-}\textgreater{} |onManualPublish| TickerActionsContext}
\end{Highlighting}
\end{Shaded}

Die drei Panels sind physisch getrennte React-Komponenten, kommunizieren
aber ausschließlich über Context-Provider --- kein Prop-Drilling über
Komponentengrenzen hinweg.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{React Context-Architektur}\label{react-context-architektur}

Das Frontend verwendet drei spezialisierte Contexts, die in
\texttt{LiveTicker} zusammengeführt werden. Jeder Context hat eine klar
definierte Verantwortung:

\textbf{\texttt{TickerModeContext}} --- Modus-State und
Tastatur-Aktionen:

\begin{Shaded}
\begin{Highlighting}[]
\KeywordTok{interface}\NormalTok{ TickerModeContextValue \{}
\NormalTok{  mode}\OperatorTok{:}\NormalTok{ TickerMode}\OperatorTok{;} \CommentTok{// "auto" | "coop" | "manual"}
\NormalTok{  setMode}\OperatorTok{:}\NormalTok{ (mode}\OperatorTok{:}\NormalTok{ TickerMode) }\KeywordTok{=\textgreater{}} \DataTypeTok{void}\OperatorTok{;}
\NormalTok{  acceptDraft}\OperatorTok{:}\NormalTok{ () }\KeywordTok{=\textgreater{}} \BuiltInTok{Promise}\OperatorTok{\textless{}}\DataTypeTok{void}\OperatorTok{\textgreater{};} \CommentTok{// TAB{-}Shortcut}
\NormalTok{  rejectDraft}\OperatorTok{:}\NormalTok{ () }\KeywordTok{=\textgreater{}} \BuiltInTok{Promise}\OperatorTok{\textless{}}\DataTypeTok{void}\OperatorTok{\textgreater{};} \CommentTok{// ESC{-}Shortcut}
\NormalTok{\}}
\end{Highlighting}
\end{Shaded}

\textbf{\texttt{TickerDataContext}} --- Match-Daten und
Reload-Funktionen:

\begin{Shaded}
\begin{Highlighting}[]
\KeywordTok{interface}\NormalTok{ TickerDataContextValue \{}
\NormalTok{  match}\OperatorTok{:}\NormalTok{ Match }\OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{  events}\OperatorTok{:}\NormalTok{ MatchEvent[]}\OperatorTok{;}
\NormalTok{  tickerTexts}\OperatorTok{:}\NormalTok{ TickerEntry[]}\OperatorTok{;}
\NormalTok{  prematch}\OperatorTok{:}\NormalTok{ TickerEntry[]}\OperatorTok{;}
\NormalTok{  lineups}\OperatorTok{:}\NormalTok{ LineupEntry[]}\OperatorTok{;}
\NormalTok{  matchStats}\OperatorTok{:}\NormalTok{ MatchStat[]}\OperatorTok{;}
\NormalTok{  players}\OperatorTok{:}\NormalTok{ Player[]}\OperatorTok{;}
\NormalTok{  playerStats}\OperatorTok{:}\NormalTok{ PlayerStat[]}\OperatorTok{;}
\NormalTok{  injuries}\OperatorTok{:}\NormalTok{ InjuryGroup[]}\OperatorTok{;}
\NormalTok{  reload}\OperatorTok{:}\NormalTok{ ReloadFunctions}\OperatorTok{;}
\NormalTok{  generatingId}\OperatorTok{:} \DataTypeTok{number} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{\}}
\end{Highlighting}
\end{Shaded}

\textbf{\texttt{TickerActionsContext}} --- Callbacks für redaktionelle
Aktionen:

\begin{Shaded}
\begin{Highlighting}[]
\KeywordTok{interface}\NormalTok{ TickerActionsContextValue \{}
\NormalTok{  onGenerate}\OperatorTok{:}\NormalTok{ (eventId}\OperatorTok{:} \DataTypeTok{number}\OperatorTok{,}\NormalTok{ style}\OperatorTok{:}\NormalTok{ TickerStyle) }\KeywordTok{=\textgreater{}} \BuiltInTok{Promise}\OperatorTok{\textless{}}\DataTypeTok{void}\OperatorTok{\textgreater{};}
\NormalTok{  onManualPublish}\OperatorTok{:}\NormalTok{ (text}\OperatorTok{,}\NormalTok{ icon}\OperatorTok{?,}\NormalTok{ minute}\OperatorTok{?,}\NormalTok{ phase}\OperatorTok{?,}\NormalTok{ rawInput}\OperatorTok{?}\NormalTok{) }\KeywordTok{=\textgreater{}} \BuiltInTok{Promise}\OperatorTok{\textless{}}\DataTypeTok{void}\OperatorTok{\textgreater{};}
\NormalTok{  onDraftActive}\OperatorTok{:}\NormalTok{ (id}\OperatorTok{:} \DataTypeTok{number}\OperatorTok{,}\NormalTok{ text}\OperatorTok{:} \DataTypeTok{string}\NormalTok{) }\KeywordTok{=\textgreater{}} \DataTypeTok{void}\OperatorTok{;}
\NormalTok{  onPublished}\OperatorTok{:}\NormalTok{ (id}\OperatorTok{:} \DataTypeTok{number}\OperatorTok{,}\NormalTok{ text}\OperatorTok{:} \DataTypeTok{string}\OperatorTok{,}\NormalTok{ isManual}\OperatorTok{?:} \DataTypeTok{boolean}\NormalTok{) }\KeywordTok{=\textgreater{}} \DataTypeTok{void}\OperatorTok{;}
\NormalTok{  onEditEntry}\OperatorTok{:}\NormalTok{ (id}\OperatorTok{:} \DataTypeTok{number}\OperatorTok{,}\NormalTok{ text}\OperatorTok{:} \DataTypeTok{string}\NormalTok{) }\KeywordTok{=\textgreater{}} \BuiltInTok{Promise}\OperatorTok{\textless{}}\DataTypeTok{void}\OperatorTok{\textgreater{};}
\NormalTok{  onDeleteEntry}\OperatorTok{:}\NormalTok{ (id}\OperatorTok{:} \DataTypeTok{number}\NormalTok{) }\KeywordTok{=\textgreater{}} \BuiltInTok{Promise}\OperatorTok{\textless{}}\DataTypeTok{void}\OperatorTok{\textgreater{};}
\NormalTok{  retractedText}\OperatorTok{:} \DataTypeTok{string} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{  clearRetractedText}\OperatorTok{:}\NormalTok{ () }\KeywordTok{=\textgreater{}} \DataTypeTok{void}\OperatorTok{;}
\NormalTok{\}}
\end{Highlighting}
\end{Shaded}

Die Trennung von Daten- und Aktions-Context hat einen konkreten
Performance-Vorteil: Komponenten, die nur Actions konsumieren (z. B.
\texttt{EntryEditor}), re-rendern nicht bei Änderungen an
\texttt{tickerTexts}. Dies ist bei einem Live-Ticker mit
5-Sekunden-Polling relevant.

Alle Contexts exportieren zusätzlich einen Hook
(\texttt{useTickerModeContext}, \texttt{useTickerDataContext},
\texttt{useTickerActionsContext}), der bei Verwendung außerhalb des
Providers einen klaren Fehler wirft --- ein Entwicklungssicherheitsnetz.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Hook-Architektur}\label{hook-architektur}

Die Zustandslogik ist in zwei Ebenen von Hooks organisiert: gemeinsame
Hooks unter \texttt{src/hooks/} und feature-spezifische Hooks unter
\texttt{components/LiveTicker/hooks/}.

Die wichtigsten Hooks und ihre Verantwortlichkeiten:

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.2043}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.0968}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.6989}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Hook
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Paket
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Verantwortlichkeit
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{useNavigation} & hooks/ & Land→Team→Wettbewerb→Spieltag→Spiel
Navigationskette \\
\texttt{useMatchData} & hooks/ & Aggregiert die drei Polling-Hooks und
koordiniert Reload-Zyklen \\
\texttt{useMatchCore} & hooks/ & Polling der Match-Metadaten (Teams,
Status, Spielstand, Minute) \\
\texttt{useMatchEvents} & hooks/ & Polling der Spielereignisse (Tore,
Karten, Auswechslungen) \\
\texttt{useMatchTicker} & hooks/ & Polling der Ticker-Einträge
(Entwürfe, publizierte Texte) \\
\texttt{useMatchTriggers} & hooks/ & n8n-Webhooks nach Matchauswahl und
Statuswechseln \\
\texttt{useTickerMode} & hooks/ & \texttt{mode}-State, PATCH zum
Backend, Keyboard-Shortcuts \\
\texttt{useMediaWebSocket} & hooks/ & WebSocket-Verbindung mit
exponentiellem Reconnect-Backoff \\
\texttt{usePanelResize} & hooks/ & Drag-Resize zwischen Center- und
Right-Panel \\
\texttt{useApiStatus} & hooks/ & Health-Check gegen Backend (alle 30
s) \\
\texttt{useTicker} & LT/hooks/ & Ticker-Actions, Draft-Queue,
Publish-Toast (Undo) \\
\texttt{useEventDraft} & LT/hooks/ & Entwurfsansicht,
Accept/Reject-Logik \\
\texttt{useAutoPublisher} & LT/hooks/ & Automatisches Publizieren im
Auto-Modus \\
\texttt{useRightPanelData} & LT/hooks/ & Aufbereitung von Lineups,
Stats, Spielerinfo für das rechte Panel \\
\end{longtable}
}

\texttt{useMatchCore}, \texttt{useMatchEvents} und
\texttt{useMatchTicker} betreiben jeweils einen eigenen
\texttt{setInterval}-Zyklus, der bei Mount startet und bei Unmount
gestoppt wird. Die HTTP-Kommunikation erfolgt über \textbf{Axios} als
HTTP-Client. \texttt{useMatchData} aggregiert die drei Hooks und stellt
die Daten über \texttt{TickerDataContext} bereit.

\texttt{useMatchData} steuert das Polling-Intervall über die
Hilfsfunktion \texttt{resolvePollingInterval()}:

\begin{Shaded}
\begin{Highlighting}[]
\CommentTok{// src/utils/resolvePollingInterval.ts}
\ImportTok{export} \KeywordTok{function} \FunctionTok{resolvePollingInterval}\NormalTok{(matchState}\OperatorTok{:} \DataTypeTok{string} \OperatorTok{|} \DataTypeTok{null}\NormalTok{)}\OperatorTok{:} \DataTypeTok{number}\NormalTok{ \{}
  \ControlFlowTok{if}\NormalTok{ (matchState }\OperatorTok{===} \StringTok{"Live"} \OperatorTok{||}\NormalTok{ matchState }\OperatorTok{===} \StringTok{"FullTime"}\NormalTok{) }\ControlFlowTok{return}\NormalTok{ POLL\_EVENTS\_MS}\OperatorTok{;} \CommentTok{// 5000}
  \ControlFlowTok{if}\NormalTok{ (matchState }\OperatorTok{==} \KeywordTok{null}\NormalTok{) }\ControlFlowTok{return}\NormalTok{ POLL\_EVENTS\_MS}\OperatorTok{;} \CommentTok{// 5000}
  \ControlFlowTok{return}\NormalTok{ POLL\_PREMATCH\_MS}\OperatorTok{;} \CommentTok{// 5000}
\NormalTok{\}}
\end{Highlighting}
\end{Shaded}

Aktuell sind beide Konstanten (\texttt{POLL\_EVENTS\_MS},
\texttt{POLL\_PREMATCH\_MS}) auf 5000 ms gesetzt, sodass alle Zustände
mit einem einheitlichen 5-Sekunden-Intervall gepollt werden. Die
Unterscheidung ist als Erweiterungspunkt vorgesehen, um bei Bedarf
ruhigere Polling-Intervalle für inaktive Spiele einzuführen.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{Slash-Command-Parser
(\texttt{parseCommand.ts})}{Slash-Command-Parser (parseCommand.ts)}}\label{slash-command-parser-parsecommand.ts}

Der Slash-Command-Parser ermöglicht schnelle manuelle Texteingabe ohne
Maus-Interaktion. Er übersetzt kurze Kommandos in formatierte
Ticker-Texte und liefert Metadaten (Icon, Phase, Minute) für die
Publikation.

Die Funktion \texttt{parseCommand(input,\ currentMinute)} gibt ein
typisiertes \texttt{ParseResult} zurück:

\begin{Shaded}
\begin{Highlighting}[]
\ImportTok{export} \KeywordTok{interface}\NormalTok{ ParseResult \{}
\NormalTok{  type}\OperatorTok{:} \DataTypeTok{string}\OperatorTok{;}
\NormalTok{  formatted}\OperatorTok{:} \DataTypeTok{string}\OperatorTok{;}
\NormalTok{  warnings}\OperatorTok{:} \DataTypeTok{string}\NormalTok{[]}\OperatorTok{;}
\NormalTok{  isValid}\OperatorTok{:} \DataTypeTok{boolean}\OperatorTok{;}
\NormalTok{  meta}\OperatorTok{:}\NormalTok{ \{}
\NormalTok{    icon}\OperatorTok{:} \DataTypeTok{string}\OperatorTok{;}
\NormalTok{    phase}\OperatorTok{:} \DataTypeTok{string} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{    minute}\OperatorTok{:} \DataTypeTok{number} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{  \}}\OperatorTok{;}
\NormalTok{\}}
\end{Highlighting}
\end{Shaded}

\textbf{Phasen-Commands} (11 Einträge in \texttt{PHASE\_CMDS}) erzeugen
vordefinierte Texte mit optionaler Minutenangabe. Die folgende Tabelle
zeigt eine Auswahl der häufigsten Einträge (7 von 11):

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}llll@{}}
\toprule\noalign{}
Command & Ausgabe & Phase & Icon \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{/prematch} & „Vorbericht'' & \texttt{Before} & 📣 \\
\texttt{/anpfiff} & „Anpfiff!'' & \texttt{FirstHalf} & 📣 \\
\texttt{/hz} & „Halbzeit!'' & \texttt{FirstHalfBreak} & 🔔 \\
\texttt{/2hz} & „Anstoß zur 2. Halbzeit'' & \texttt{SecondHalf} & 📣 \\
\texttt{/vz1} & „Beginn der Verlängerung'' & \texttt{ExtraFirstHalf} &
📣 \\
\texttt{/elfmeter} & „Elfmeterschießen beginnt'' &
\texttt{PenaltyShootout} & 🥅 \\
\texttt{/abpfiff} & „Abpfiff!'' & \texttt{After} & 📣 \\
\end{longtable}
}

\textbf{Event-Commands} (12 Einträge in \texttt{CMD\_MAP}, 8 Typen nach
Expansion) erzeugen strukturierte Texte mit Validierungshinweisen:

\begin{verbatim}
/g Mustermann EF  → "TOR — Mustermann (EF)"          + icon [Tor]
/gelb Müller EF   → "Gelb — Müller (EF)"             + icon [Gelb]
/rot Huber Bayern → "Rote Karte — Huber (Bayern)"    + icon [Rot]
/s EinRaus AusRein EF → "Wechsel — EinRaus <-> AusRein (EF)" + icon [Wechsel]
/n Freistoß knapp über das Tor → "Freistoß knapp über das Tor"
\end{verbatim}

Für unvollständige Eingaben gibt \texttt{warnings{[}{]}} gezielte
Hinweise zurück (\texttt{"Fehlend:\ Spieler"}), die im
\texttt{EntryEditor} live angezeigt werden. Das Frontend kann damit ohne
Backend-Aufruf Validierungsfeedback geben.

Nicht erkannte Commands werden mit \texttt{isValid:\ false} und einer
Fehlermeldung zurückgegeben (\texttt{"Unbekannter\ Command:\ /xyz"}),
statt einen Laufzeitfehler zu werfen.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{WebSocket-Hook
(\texttt{useMediaWebSocket})}{WebSocket-Hook (useMediaWebSocket)}}\label{websocket-hook-usemediawebsocket}

Der \texttt{useMediaWebSocket}-Hook verwaltet die Lebensdauer der
WebSocket-Verbindung zum Backend. Die Kernentscheidung ist das
\textbf{exponentielle Reconnect-Backoff}:

\begin{Shaded}
\begin{Highlighting}[]
\KeywordTok{const}\NormalTok{ delay }\OperatorTok{=} \BuiltInTok{Math}\OperatorTok{.}\FunctionTok{min}\NormalTok{(}
\NormalTok{  BASE\_DELAY\_MS }\OperatorTok{*} \DecValTok{2} \OperatorTok{**}\NormalTok{ retryCountRef}\OperatorTok{.}\AttributeTok{current}\OperatorTok{,} \CommentTok{// 1s → 2s → 4s → 8s → …}
\NormalTok{  MAX\_DELAY\_MS}\OperatorTok{,} \CommentTok{// max 30s}
\NormalTok{)}\OperatorTok{;}
\end{Highlighting}
\end{Shaded}

Der \texttt{retryCountRef} zählt aufeinanderfolgende
Verbindungsabbrüche. Bei erfolgreicher Reconnection wird er auf 0
zurückgesetzt. \texttt{BASE\_DELAY\_MS\ =\ 1000},
\texttt{MAX\_DELAY\_MS\ =\ 30\_000}.

Ein \texttt{mountedRef} verhindert Zustandsänderungen nach dem Unmounten
der Komponente --- ein häufiges React-Problem bei asynchronen
Operationen:

\begin{Shaded}
\begin{Highlighting}[]
\NormalTok{ws}\OperatorTok{.}\AttributeTok{onclose} \OperatorTok{=}\NormalTok{ () }\KeywordTok{=\textgreater{}}\NormalTok{ \{}
  \ControlFlowTok{if}\NormalTok{ (}\OperatorTok{!}\NormalTok{mountedRef}\OperatorTok{.}\AttributeTok{current}\NormalTok{) }\ControlFlowTok{return}\OperatorTok{;} \CommentTok{// Kein setState nach Unmount}
  \CommentTok{// … retry{-}Logik}
\NormalTok{\}}\OperatorTok{;}
\end{Highlighting}
\end{Shaded}

Der Cleanup im \texttt{useEffect} setzt \texttt{ws.onclose\ =\ null},
bevor die WebSocket geschlossen wird. Ohne diesen Schritt würde das
\texttt{close}-Event des kontrollierten Schließens einen
unbeabsichtigten Reconnect-Versuch auslösen.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Modusimplementierung im
Frontend}\label{modusimplementierung-im-frontend}

Der aktive Modus (\texttt{auto} / \texttt{coop} / \texttt{manual})
steuert das Verhalten mehrerer Frontend-Komponenten (vgl. Kap. 4.6.4 für
die konzeptionelle Einordnung der Moduslogik und Interaktionsdesign).
Die Implementierung verteilt sich auf drei Ebenen:

\textbf{Persistenz und Synchronisation} --- \texttt{useTickerMode}
initialisiert den Modus lokal mit \texttt{coop} als Standardwert und
überschreibt diesen beim Laden eines Spiels mit dem in der Datenbank
gespeicherten \texttt{ticker\_mode}. Änderungen werden über
\texttt{PATCH\ /matches/\{id\}/ticker-mode} ans Backend
zurückgeschrieben. Lokale Optimistic-Updates vermeiden wahrnehmbare
Latenz beim Umschalten.

\textbf{Keyboard-Shortcuts} --- Im Co-op-Modus registriert
\texttt{useTickerMode} die Shortcuts \texttt{TAB} (acceptDraft) und
\texttt{ESC} (rejectDraft) direkt als Keyboard-Event-Listener. Die
Shortcut-Registrierung ist an den Modus-Zustand gebunden und wird bei
Moduswechsel automatisch aktualisiert.

\textbf{\texttt{useAutoPublisher}} --- Im Auto-Modus überwacht dieser
Hook die \texttt{tickerTexts} auf neue Draft-Einträge
(\texttt{status="draft"}) und publiziert sie automatisch. Dies ist als
Frontend-Fallback gedacht: Primär setzt n8n mit
\texttt{auto\_publish=True} den Status direkt beim Generieren;
\texttt{useAutoPublisher} fängt Fälle auf, in denen das nicht geschehen
ist.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{TypeScript-Migration}\label{typescript-migration}

Das Frontend wurde im Verlauf des Projekts von reinem JavaScript auf
TypeScript migriert. Ziel war nicht die vollständige Auflösung aller
\texttt{any}-Typen, sondern eine pragmatische Typisierung der
systemkritischen Pfade mit messbaren Qualitätskennzahlen.

\textbf{Migrationsstrategie} --- Die Migration folgte einem
schrittweisen Ansatz: Zuerst wurden alle \texttt{.js}- und
\texttt{.jsx}-Dateien in \texttt{.ts} und \texttt{.tsx} umbenannt, dann
\texttt{tsconfig.json} konfiguriert (\texttt{target:\ es2015},
\texttt{module:\ esnext}, \texttt{jsx:\ react-jsx},
\texttt{moduleResolution:\ node}). Anschließend wurden TypeScript-Fehler
von innen nach außen behoben --- beginnend mit dem Typ-System
(\texttt{src/types/index.ts}) über Contexts bis zu den Komponenten. Der
\texttt{strict}-Modus ist derzeit deaktiviert
(\texttt{"strict":\ false}), um die inkrementelle Migration ohne
blockierende Fehlereskalation zu ermöglichen.

\textbf{Zentrale Typ-Definitionen} --- \texttt{src/types/index.ts}
definiert die gemeinsamen Domain-Interfaces:

\begin{Shaded}
\begin{Highlighting}[]
\ImportTok{export type}\NormalTok{ TickerMode }\OperatorTok{=} \StringTok{"auto"} \OperatorTok{|} \StringTok{"coop"} \OperatorTok{|} \StringTok{"manual"}\OperatorTok{;}
\ImportTok{export type}\NormalTok{ TickerStyle }\OperatorTok{=} \StringTok{"neutral"} \OperatorTok{|} \StringTok{"euphorisch"} \OperatorTok{|} \StringTok{"kritisch"}\OperatorTok{;}
\ImportTok{export type}\NormalTok{ MatchPhase }\OperatorTok{=} \StringTok{"Before"} \OperatorTok{|} \StringTok{"FirstHalf"} \OperatorTok{|} \StringTok{"FirstHalfBreak"} \OperatorTok{|} \StringTok{"SecondHalf"} \OperatorTok{|} \OperatorTok{...;}

\ImportTok{export} \KeywordTok{interface}\NormalTok{ TickerEntry \{}
\NormalTok{  id}\OperatorTok{:} \DataTypeTok{number}\OperatorTok{;}
\NormalTok{  match\_id}\OperatorTok{:} \DataTypeTok{number}\OperatorTok{;}
\NormalTok{  event\_id}\OperatorTok{?:} \DataTypeTok{number} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{  text}\OperatorTok{:} \DataTypeTok{string}\OperatorTok{;}
\NormalTok{  style}\OperatorTok{?:} \DataTypeTok{string} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{  status}\OperatorTok{:} \StringTok{"draft"} \OperatorTok{|} \StringTok{"published"} \OperatorTok{|} \StringTok{"rejected"}\OperatorTok{;}
\NormalTok{  source}\OperatorTok{?:} \StringTok{"ai"} \OperatorTok{|} \StringTok{"manual"}\OperatorTok{;}
\NormalTok{  minute}\OperatorTok{?:} \DataTypeTok{number} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{  phase}\OperatorTok{?:} \DataTypeTok{string} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{  icon}\OperatorTok{?:} \DataTypeTok{string} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{  image\_url}\OperatorTok{?:} \DataTypeTok{string} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{  video\_url}\OperatorTok{?:} \DataTypeTok{string} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{  llm\_model}\OperatorTok{?:} \DataTypeTok{string} \OperatorTok{|} \DataTypeTok{null}\OperatorTok{;}
\NormalTok{\}}
\end{Highlighting}
\end{Shaded}

\textbf{Ergebnis} --- Nach der Migration erzielt das Projekt \textbf{0
TypeScript-Compilerfehler} und eine \textbf{type-coverage von 95,84 \%}
(gemessen mit \texttt{type-coverage\ -\/-strict}). Die detaillierte
Darstellung der Migrationsergebnisse und die Analyse der verbleibenden
untypisierten Stellen folgt in Abschnitt 6.2.5.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{n8n
Workflow-Implementierung}\label{n8n-workflow-implementierung}

Die n8n-Workflows bilden die Orchestrierungsschicht zwischen externen
Datenquellen, dem FastAPI-Backend und dem LLM-Dienst. Alle 16
Workflow-Dateien liegen als versionierte JSON-Exporte im
Projektverzeichnis \texttt{n8n/} vor und können direkt in eine
n8n-Instanz importiert werden. Die Workflows gliedern sich in sechs
funktionale Gruppen:

\textbf{A) Stammdaten und Matchstruktur} --- Die Workflows
\texttt{01\_import\_countries}, \texttt{02\_import\_teams\_by\_country},
\texttt{03\_import\_competitions\_and\_matches} und
\texttt{04\_import\_matches} laden Länder, Teams, Wettbewerbe und
Spielpläne aus API-Football und persistieren diese via
Upsert-Strategien. Externe IDs dienen als stabile Zuordnungsschlüssel
für konfliktfreie Aktualisierungen.

\textbf{B) Spielbezogene Detaildaten} --- \texttt{04\_import\_lineups},
\texttt{05\_import\_match\_statistics},
\texttt{06\_import\_player\_statistics} und
\texttt{07\_import\_prematch} importieren Aufstellungen, Match- und
Spielerstatistiken matchbezogen. Der Prematch-Workflow erzeugt
zusätzlich strukturierte Kontextdaten (Verletzungen, Head-to-Head,
Tabellenstand) als synthetische Events, die dem LLM als Promptkontext
dienen.

\textbf{C) KI-Generierung} --- \texttt{09\_events\_llm\_workflow},
\texttt{13\_Halftime\_aftertime} und \texttt{14\_Game\_ANpfiff\_ABpfiff}
importieren Live-Ereignisse, erzeugen synthetische Phasenereignisse und
delegieren die Textgenerierung an das Backend. Der
Zusammenfassungs-Workflow (\texttt{13}) ruft den LLM-Provider direkt
auf, da Halbzeit-/Abpfiff-Zusammenfassungen einen umfangreicheren Prompt
mit Statistiken erfordern.

\textbf{D) Medien und Social Media} ---
\texttt{08\_scoreplay\_media\_workflow}, \texttt{10\_Twitter},
\texttt{11\_youtube} und \texttt{12\_insta} importieren Medien- und
Social-Media-Inhalte. Der Media-Workflow übergibt Bilder an das Backend,
das diese über WebSocket verteilt; die Social-Media-Workflows speichern
Clips in der Datenbank. Diese Workflows dienen der Ingestion, nicht dem
Publishing.

\textbf{E) Live-Stats-Monitor} --- \texttt{15\_live\_stats\_monitor}
überwacht laufende Spiele per Webhook und aktualisiert Statistiken in
Echtzeit. Der Workflow prüft, ob ein Spiel aktiv ist, und delegiert die
Aktualisierung an den entsprechenden Backend-Endpunkt.

\textbf{F) Aktuelle-Minute-Update} ---
\texttt{16\_aktuelle\_minute\_livespiel} empfängt minutengenaue
Statusupdates aus der Football-API, ermittelt Phase und Zustand des
Spiels (\texttt{1H}/\texttt{2H}/\texttt{HT}/\texttt{FT} u.\,a.) und
aktualisiert \texttt{minute}, \texttt{match\_phase} und
\texttt{match\_state} in der Datenbank. Bei Phasenwechsel triggert der
Workflow automatisch den \texttt{match-status}-Webhook.

Dieser Abschnitt dokumentiert die Implementierungsdetails der zentralen
Workflows (Gruppen B--D).

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{Events-LLM-Workflow
(\texttt{09\_events\_llm\_workflow.json})}{Events-LLM-Workflow (09\_events\_llm\_workflow.json)}}\label{events-llm-workflow-09_events_llm_workflow.json}

Workflow \texttt{09} ist der kritischste im System: Er importiert
Live-Ereignisse via Webhook (\texttt{POST\ /Events}), persistiert sie
per UPSERT (\texttt{ON\ CONFLICT\ (source\_id)\ DO\ NOTHING}) und
triggert die KI-Generierung für jeden neuen Event. Eine initiale
SQL-Abfrage bestimmt anhand der Teamzugehörigkeit, ob die Instanz
\texttt{ef\_whitelabel} oder \texttt{generic} und der Stil
\texttt{euphorisch} oder \texttt{neutral} ist; \texttt{auto\_publish}
wird direkt aus \texttt{ticker\_mode} des Spiels gesetzt. Nur Events mit
erfolgreicher Datenbankzeile (zurückgegebene \texttt{id}, kein
\texttt{DO\ NOTHING}-Konflikt) passieren den Filter-Knoten und triggern
den Backend-Endpunkt. Ein zusätzlicher EF-spezifischer Zweig liest
Spielerdaten von \texttt{profis.eintracht.de}, baut S3-Video-URLs aus
neunstellig aufgefüllten Spieler-IDs auf und persistiert Torjubel-Videos
als Ticker-Einträge.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{Prematch-Import-Workflow
(\texttt{07\_import\_prematch.json})}{Prematch-Import-Workflow (07\_import\_prematch.json)}}\label{prematch-import-workflow-07_import_prematch.json}

Workflow \texttt{07} baut den Vorberichtskontext aus fünf parallelen
Football-API-Abfragen auf (Verletzungen, Head-to-Head, Teamstatistiken
Heim/Gast, Tabellenstand) und persistiert die Ergebnisse als
\texttt{synthetic\_events} mit idempotenter
\texttt{ON\ CONFLICT\ (match\_id,\ type)\ DO\ UPDATE}-Strategie. Ein
LLM-Trigger wird nur ausgelöst, wenn noch kein nicht-verworfener
Ticker-Eintrag für das jeweilige synthetische Event existiert
(\texttt{WHERE\ te.id\ IS\ NULL}) --- bereits generierte und eventuell
publizierte Texte werden damit nicht überschrieben.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{Matchphasen-Workflow
(\texttt{14\_Game\_ANpfiff\_ABpfiff.json})}{Matchphasen-Workflow (14\_Game\_ANpfiff\_ABpfiff.json)}}\label{matchphasen-workflow-14_game_anpfiff_abpfiff.json}

Workflow \texttt{14} verarbeitet Spielzustands-Übergänge (Anpfiff,
Halbzeit, Abpfiff etc.) via Webhook (\texttt{POST\ /match-status}). Ein
JavaScript-Knoten validiert Zustandsübergänge gegen eine Matrix
erlaubter Vorzustände --- ungültige Übergänge (z. B. direkt von
\texttt{PreMatch} zu \texttt{2H}) werden zurückgewiesen. Für
Vollzeit-Ereignisse (\texttt{FT}, \texttt{AET}, \texttt{PEN}) generiert
der Workflow die gesamte Phasensequenz rückwirkend: Ein
\texttt{FT}-Signal erzeugt vier synthetische Events (Anstoß, Halbzeit,
2. Halbzeit, Abpfiff). Das zentrale SQL-Statement kombiniert
Match-Update, Event-Insert und eine \texttt{demote}-CTE in einer
einzigen Transaktion --- Letztere stuft bereits publizierte Phasen-Texte
bei Re-Triggers auf \texttt{draft} zurück, sodass die Redaktion sie
erneut prüfen kann.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{Halbzeit/Abpfiff-Zusammenfassung
(\texttt{13\_Halftime\_aftertime.json})}{Halbzeit/Abpfiff-Zusammenfassung (13\_Halftime\_aftertime.json)}}\label{halbzeitabpfiff-zusammenfassung-13_halftime_aftertime.json}

Workflow \texttt{13} erzeugt narrative Zusammenfassungen für Halbzeit
und Abpfiff. Im Gegensatz zu \texttt{09} ruft er OpenRouter
(\texttt{google/gemini-2.0-flash-lite-001}) \textbf{direkt} auf, da
Zusammenfassungen keinen Few-Shot-Stil aus der Datenbank benötigen,
sondern einen umfangreicheren Prompt mit parallel geladenen Statistiken
(Ballbesitz, Schüsse, Pässe, Spieler-Ratings) erfordern. Das Ergebnis
wird über \texttt{POST\ /api/v1/ticker/manual} als Ticker-Eintrag
gespeichert --- je nach \texttt{ticker\_mode} direkt als
\texttt{published} oder als \texttt{draft}.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{\texorpdfstring{ScorePlay-Medien-Workflow
(\texttt{08\_scoreplay\_media\_workflow.json})}{ScorePlay-Medien-Workflow (08\_scoreplay\_media\_workflow.json)}}\label{scoreplay-medien-workflow-08_scoreplay_media_workflow.json}

Workflow \texttt{08} sucht Medien-Assets bei ScorePlay für Spieler eines
Torereignisses. Die Spieler-Suche (\texttt{GET\ /v1/tag/search})
normalisiert Umlaute (ä→a, ö→o, ü→u, ß→ss) und matcht gegen vier
Namensfelder (\texttt{full\_name}, \texttt{first\_name},
\texttt{last\_name}, \texttt{ai\_name}). Gefundene Thumbnail-,
Compressed- und Original-URLs werden an
\texttt{POST\ /api/v1/media/incoming} übertragen und vom Backend per
WebSocket an verbundene Frontend-Clients verteilt.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Implementierungsübergreifende
Muster}\label{implementierungsuxfcbergreifende-muster}

Über alle Workflows hinweg lassen sich fünf Implementierungsmuster
identifizieren:

\textbf{1. Idempotente UPSERT-Strategie} --- Alle Insert-Operationen
verwenden \texttt{ON\ CONFLICT\ DO\ UPDATE} oder \texttt{DO\ NOTHING}.
Als Konfliktschlüssel dienen externe IDs (\texttt{source\_id},
\texttt{external\_id}, \texttt{vid}), zusammengesetzte Schlüssel
(\texttt{match\_id,\ type}) oder Unique-Constraints auf Namensfeldern.
Das macht alle Workflows bei Mehrfachausführung sicher.

\textbf{2. Filter-vor-Trigger-Muster} --- Vor jedem LLM-Trigger steht
ein Filter-Knoten, der nur Rows mit einer \texttt{id} (erfolgreicher
DB-Insert) durchlässt. Events mit \texttt{DO\ NOTHING}-Konflikten
(bereits importiert) erzeugen keinen neuen LLM-Aufruf.

\textbf{3. Statusbasierte Filterung} --- Bei synthetischen Events und
Ticker-Einträgen werden vorhandene Zustände berücksichtigt, um
redundante Generierungen zu vermeiden: Ein LLM-Aufruf wird nur
getriggert, wenn noch kein nicht-verworfener Ticker-Eintrag für das
Event existiert (vgl. die WHERE-Bedingungen in 5.5.2 und 5.5.3). Bei
Re-Triggern werden bereits publizierte Phasen-Texte auf \texttt{draft}
zurückgestuft (\texttt{demote}-CTE in 5.5.3).

\textbf{4. Ticker-Mode-Propagation} --- \texttt{ticker\_mode} wird in
allen Generierungsworkflows als \texttt{auto\_publish}-Flag an das
Backend weitergegeben (vgl. Kap. 5.3.3). Die Moduslogik sitzt damit
vollständig in der Datenbank; n8n liest sie bei jedem Aufruf neu aus.

\textbf{5. Instanz-Routing} --- Die Unterscheidung zwischen
\texttt{ef\_whitelabel} und \texttt{generic} erfolgt per
\texttt{ILIKE\ \textquotesingle{}\%Frankfurt\%\textquotesingle{}}-Abfrage
auf Teamnamen. Das Ergebnis steuert Stilwahl, Few-Shot-Auswahl im
Backend und ob der Torjubel-Video-Pfad aktiviert wird.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Qualitätssicherung und
Tests}\label{qualituxe4tssicherung-und-tests}

Die Qualitätssicherung folgt dem Testpyramiden-Modell nach Cohn (2009)
mit drei Ebenen: Unit-Tests, Integrations-Tests und End-to-End-Tests.
Insgesamt umfasst die Testsuite \textbf{391 Tests} (187 Frontend, 198
Backend, 6 E2E), die alle grün durchlaufen.

\textbf{Frontend-Tests} --- Das Frontend verwendet Jest mit
\texttt{@testing-library/react} für 187 Tests in 15 Dateien. Die
Testdateien sind kolokiert mit ihrem Quellcode: Komponentendateien unter
\texttt{components/LiveTicker/components/}, Hook-Tests unter
\texttt{hooks/} und Utility-Tests unter \texttt{utils/}. Besonders
umfangreich getestet ist der \texttt{parseCommand}-Parser (45
Testfälle), da er die kritische manuelle Eingabeschicht bildet. Ein
exemplarischer Testfall illustriert das Prüfmuster:

\begin{Shaded}
\begin{Highlighting}[]
\FunctionTok{test}\NormalTok{(}\StringTok{"vollstaendiger Command ist valid"}\OperatorTok{,}\NormalTok{ () }\KeywordTok{=\textgreater{}}\NormalTok{ \{}
  \KeywordTok{const}\NormalTok{ result }\OperatorTok{=} \FunctionTok{parseCommand}\NormalTok{(}\StringTok{"/g Muller EIN"}\OperatorTok{,} \DecValTok{32}\NormalTok{)}\OperatorTok{;}
  \FunctionTok{expect}\NormalTok{(result}\OperatorTok{.}\AttributeTok{isValid}\NormalTok{)}\OperatorTok{.}\FunctionTok{toBe}\NormalTok{(}\KeywordTok{true}\NormalTok{)}\OperatorTok{;}
  \FunctionTok{expect}\NormalTok{(result}\OperatorTok{.}\AttributeTok{type}\NormalTok{)}\OperatorTok{.}\FunctionTok{toBe}\NormalTok{(}\StringTok{"goal"}\NormalTok{)}\OperatorTok{;}
  \FunctionTok{expect}\NormalTok{(result}\OperatorTok{.}\AttributeTok{formatted}\NormalTok{)}\OperatorTok{.}\FunctionTok{toBe}\NormalTok{(}\StringTok{"TOR {-}{-} Muller (EIN)"}\NormalTok{)}\OperatorTok{;}
  \FunctionTok{expect}\NormalTok{(result}\OperatorTok{.}\AttributeTok{meta}\OperatorTok{.}\AttributeTok{icon}\NormalTok{)}\OperatorTok{.}\FunctionTok{toBe}\NormalTok{(}\StringTok{"[Tor]"}\NormalTok{)}\OperatorTok{;}
  \FunctionTok{expect}\NormalTok{(result}\OperatorTok{.}\AttributeTok{warnings}\NormalTok{)}\OperatorTok{.}\FunctionTok{toHaveLength}\NormalTok{(}\DecValTok{0}\NormalTok{)}\OperatorTok{;}
\NormalTok{\})}\OperatorTok{;}
\end{Highlighting}
\end{Shaded}

\textbf{Backend-Tests} --- Das Backend nutzt pytest mit FastAPI
\texttt{TestClient} und transaktionalem Rollback für 198 Tests bei 75 \%
Statement-Coverage. Die Tests gliedern sich in API-Integrations-Tests
(\texttt{test\_ticker\_api.py}, \texttt{test\_matches\_api.py},
\texttt{test\_events\_api.py} u. a.), die Endpunkte gegen eine
PostgreSQL-Testdatenbank prüfen, sowie Unit-Tests für Services
(\texttt{test\_llm\_service.py}, \texttt{test\_ticker\_service.py}) und
Repositories (\texttt{test\_ticker\_entry\_repository.py}). Gemeinsame
Fixtures (Datenbankverbindung, Test-Match, Test-Events) sind in
\texttt{conftest.py} zentralisiert.

\textbf{End-to-End-Tests} --- Sechs Playwright-Tests validieren den
stabilen Kern der Browser-Anwendung: korrektes Rendern, Fehlerfreiheit
beim Laden und grundlegende Interaktionspunkte.

\textbf{Evaluations-Infrastruktur} --- Ergänzend zur Testsuite enthält
\texttt{backend/app/utils/evaluation\_metrics.py} sechs statistische
Hilfsfunktionen, die speziell für die Qualitätsevaluation der
KI-Textgenerierung implementiert wurden:

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.2824}}
  >{\raggedright\arraybackslash}p{(\linewidth - 2\tabcolsep) * \real{0.7176}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Funktion
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Aufgabe
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{compute\_ttp\_stats(entries)} & Berechnet TTP-Statistiken (Mean,
Median, Perzentile) aus gemessenen Latenzen \\
\texttt{cliffs\_delta(group\_a,\ group\_b)} & Effektstärkemaß nach Cliff
(1993) für ordinal-skalierte Vergleiche (z. B. auto vs.~manual TTP) \\
\texttt{bootstrap\_ci(data,\ n\_resamples)} &
Bootstrap-Konfidenzintervall (95 \%) über nichtparametrisches
Resampling \\
\texttt{cohens\_kappa(rater\_a,\ rater\_b)} &
Inter-Rater-Übereinstimmungsmaß nach Cohen (1960) für die
Qualitätsbewertung \\
\texttt{distribution\_summary(scores)} & Deskriptive Zusammenfassung
(Mean, SD, Min, Max) für Bewertungslisten \\
\texttt{aggregate\_quality\_by\_group(entries)} & Aggregiert
Qualitätsscores nach Gruppe (Stil, Ereignistyp, Instanz) \\
\end{longtable}
}

Diese Funktionen werden in Kapitel 6.3--6.5 direkt genutzt:
\texttt{cliffs\_delta} für den TTP-Vergleich, \texttt{cohens\_kappa} für
die Bewertungsübereinstimmung und \texttt{aggregate\_quality\_by\_group}
für die profilspezifische Qualitätsanalyse. Die Implementierung in einem
dedizierten \texttt{utils}-Modul (statt im Test-Code) macht die
Statistikfunktionen wiederverwendbar --- etwa für einen zukünftigen
automatisierten Scoring-Dienst im Produktivbetrieb (vgl. Kap. 8.3.3).

Die detaillierte Auswertung aller Testmetriken, Coverage-Verteilungen
und konkreten Testbeispiele folgt in Abschnitt 6.2.

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Deployment}\label{deployment}

Das System ist als dreischichtige Cloud-Deployment auf
\textbf{Render.com} (Region: Oregon, US West) realisiert. Alle drei
Dienste werden aus demselben Repository-Branch
(\texttt{deploy/render-v2}) deployt und kommunizieren über das private
Render-Netzwerk.

\textbf{Backend (Docker Web Service)} --- Das FastAPI-Backend läuft als
Docker-Container auf einem Free-Tier-Web-Service (\texttt{0.1\ vCPU},
\texttt{512\ MB\ RAM}). Render baut das Image aus
\texttt{./backend/Dockerfile} mit \texttt{./backend} als Build-Kontext
und startet den Uvicorn-Server gemäß der \texttt{CMD}-Instruktion im
Dockerfile. Die Health-Check-Route \texttt{GET\ /health} (vgl. Abschnitt
5.2.1) wird von Render periodisch abgefragt; bei Ausfall wird der Dienst
automatisch neu gestartet. Zugangsdaten und Konfiguration werden als
Umgebungsvariablen injiziert (Datenbankverbindungs-URL, API-Keys für
LLM-Provider). Das Backend ist unter
\texttt{https://liveticker-backend.onrender.com} erreichbar.

\textbf{Datenbank (PostgreSQL 15)} --- Die Datenbankinstanz
\texttt{liveticker-db} läuft auf einem Render-Managed-PostgreSQL-Dienst
(Free Tier: \texttt{256\ MB\ RAM}, \texttt{1\ GB\ Storage}). Die
Verbindung zum Backend erfolgt über den internen Hostnamen innerhalb des
privaten Render-Netzwerks. Beim ersten Start legt
\texttt{Base.metadata.create\_all(bind=engine)} alle Tabellen an;
nachfolgende Schema-Änderungen werden via Alembic-Migrationen
eingespielt. Da Free-Tier-Instanzen keinen persistenten Prozess-Storage
vorhalten, ist alle Zustandshaltung in der Datenbank zentralisiert ---
ein direktes Ergebnis des in Abschnitt 3.8.3 beschriebenen
Stateless-Design-Prinzips.

\textbf{Frontend (Static Site)} --- Das React/TypeScript-Frontend wird
als statische Seite deployt. Render führt
\texttt{cd\ frontend\ \&\&\ npm\ ci\ \&\&\ npm\ run\ build} aus und
veröffentlicht das Verzeichnis \texttt{./frontend/build} über sein CDN.
Da kein Server-Prozess läuft, werden die generierten Dateien direkt aus
dem CDN ausgeliefert. Das Frontend ist unter
\texttt{https://liveticker-frontend.onrender.com} erreichbar.

\textbf{n8n (lokal mit ngrok)} --- Die n8n-Workflow-Instanz läuft lokal
und ist über einen \textbf{ngrok}-Tunnel für externe Webhooks
erreichbar. Ngrok stellt einen öffentlich adressierbaren HTTPS-Endpunkt
bereit, der eingehende Trigger-Anfragen (z. B. Spielereignisse vom
Football-API-Provider) an die lokale n8n-Instanz weiterleitet. Diese
Konfiguration ist für den Entwicklungs- und Evaluationsbetrieb
ausreichend; ein produktiver Dauerbetrieb erfordert eine persistente
n8n-Instanz mit fester Webhook-URL (z. B. self-hosted auf einem VPS oder
n8n Cloud).

Die vier Komponenten bilden zusammen die in Abschnitt 4.1 konzipierten
drei Systemschichten: n8n übernimmt die Datenbeschaffung (Schicht 1),
Backend und Datenbank die Anwendungs- und Persistenzlogik (Schicht 2),
das Frontend die Präsentation (Schicht 3). Damit ist die Implementierung
vollständig; Kapitel 6 dokumentiert die systematische Evaluation des so
realisierten Systems.

\tikzset{
  block/.style={rectangle, draw, fill=blue!20, text width=2.5cm, text centered, rounded corners, minimum height=1cm},
  db/.style={cylinder, draw, fill=green!20, text width=1.5cm, text centered, minimum height=1cm},
  externa/.style={rectangle, draw, fill=gray!20, text width=2.5cm, text centered, rounded corners, minimum height=0.8cm},
  arrow/.style={->, thick, >=stealth},
}

%% DIAGRAM 1: System Architecture (3-Schichten + Externe)
\newcommand{\DiagramSystemArchitektur}{%
\begin{tikzpicture}[node distance=2.5cm, auto]
  % External Services
  \node[externa] (fapi) {Football-API\\api-sports.io};
  \node[externa, below of=fapi] (llm) {LLM Provider\\OpenRouter};
  \node[externa, below of=llm] (sp) {ScorePlay\\Media};
  
  % Automation Layer
  \node[block, right of=llm] (n8n) {n8n\\Automation};
  
  % Application Layer
  \node[block, right of=n8n, xshift=1.5cm] (api) {FastAPI\\Backend};
  \node[db, below of=api, yshift=-1cm] (db) {PostgreSQL};
  
  % Frontend
  \node[block, right of=api, xshift=1.5cm] (fe) {React\\Frontend};
  
  % Connections
  \draw[arrow] (fapi) -- (n8n);
  \draw[arrow] (llm) -- (n8n);
  \draw[arrow] (sp) -- (n8n);
  \draw[arrow] (n8n) -- (api);
  \draw[arrow] (api) -- (db);
  \draw[arrow] (api) -- (fe);
  \draw[arrow] (fe) -- (api);
  
  % Labels at bottom
  \node[below of=fapi, yshift=-3.5cm] {\small\textit{Externe Dienste}};
  \node[below of=n8n, yshift=-3.5cm] {\small\textit{Automatisierung}};
  \node[below of=api, yshift=-3.5cm] {\small\textit{Anwendungslogik}};
  \node[below of=fe, yshift=-3.5cm] {\small\textit{Präsentation}};
\end{tikzpicture}%
}

%% DIAGRAM 2: Player Statistics Pipeline
\newcommand{\DiagramPlayerStatsPipeline}{%
\begin{tikzpicture}[node distance=2cm, auto]
  \node[block] (input) {Player Stats\\Input (JSON)};
  \node[block, right of=input] (parse) {Parse \&\\Normalize};
  \node[block, right of=parse] (rank) {Rank Stats\\(Percentile)};
  \node[block, right of=rank] (template) {Load Template\\(Sprache)};
  \node[block, right of=template] (fill) {Fill Variables};
  \node[block, right of=fill] (output) {Output\\(Text)};
  
  \draw[arrow] (input) -- (parse);
  \draw[arrow] (parse) -- (rank);
  \draw[arrow] (rank) -- (template);
  \draw[arrow] (template) -- (fill);
  \draw[arrow] (fill) -- (output);
\end{tikzpicture}%
}

%% DIAGRAM 3: Ticker State Transitions
\newcommand{\DiagramTickerStates}{%
\begin{tikzpicture}[node distance=2.5cm, auto]
  \tikzset{state/.style={circle, draw, fill=blue!15, text width=1.2cm, text centered, minimum size=1.5cm}}
  
  \node[state] (draft) {draft};
  \node[state, right of=draft] (published) {published};
  \node[state, right of=published] (rejected) {rejected};
  \node[state, below of=draft] (archived) {archived};
  
  \draw[arrow] (draft) -- node[above] {publish} (published);
  \draw[arrow] (draft) -- node[left] {reject} (rejected);
  \draw[arrow] (published) -- node[right] {archive} (archived);
  \draw[arrow] (rejected) -- node[below] {retry} (draft);
  \draw[arrow] (published) -- node[pos=0.5, right] {edit} (draft);
\end{tikzpicture}%
}

%% DIAGRAM 4: Event Processing Workflow
\newcommand{\DiagramEventWorkflow}{%
\begin{tikzpicture}[node distance=1.8cm, auto]
  \node[block, text width=2cm] (event) {Event\\Input};
  \node[block, right of=event, text width=2cm] (dispatch) {Dispatch\\to\\Handlers};
  \node[block, right of=dispatch, text width=2cm] (enrich) {Enrich Data};
  \node[block, right of=enrich, text width=2cm] (llm) {LLM Call};
  \node[block, right of=llm, text width=2cm] (store) {Store\\Ticker};
  
  \draw[arrow] (event) -- (dispatch);
  \draw[arrow] (dispatch) -- (enrich);
  \draw[arrow] (enrich) -- (llm);
  \draw[arrow] (llm) -- (store);
  
  % Feedback loop
  \draw[arrow, bend right=45] (store) -- node[below, pos=0.5] {broadcast} (event);
\end{tikzpicture}%
}

%% DIAGRAM 5: Logical Data Model (simplified ER)
\newcommand{\DiagramDataModel}{%
\begin{tikzpicture}[node distance=3cm, auto]
  \tikzset{entity/.style={rectangle, draw, fill=green!15, text width=1.5cm, text centered, minimum height=1cm}}
  
  \node[entity] (match) {Match};
  \node[entity, right of=match] (event) {Event};
  \node[entity, right of=event] (ticker) {Ticker};
  \node[entity, below of=match] (team) {Team};
  \node[entity, below of=event] (player) {Player};
  \node[entity, below of=ticker] (media) {Media};
  
  \draw[arrow] (match) -- node[above] {1:N} (event);
  \draw[arrow] (event) -- node[above] {1:N} (ticker);
  \draw[arrow] (match) -- node[left] {2:1} (team);
  \draw[arrow] (event) -- node[right] {M:1} (player);
  \draw[arrow] (ticker) -- node[right] {1:N} (media);
\end{tikzpicture}%
}

%% DIAGRAM 6: Communication Flow (Sequence-like but in TikZ)
\newcommand{\DiagramCommunicationFlow}{%
\begin{tikzpicture}[node distance=2.5cm, auto]
  \tikzset{actor/.style={rectangle, draw, fill=orange!15, text width=2cm, text centered, minimum height=0.8cm}}
  
  \node[actor] (fe) {React\\Frontend};
  \node[actor, right of=fe] (be) {FastAPI\\Backend};
  \node[actor, right of=be] (n8n) {n8n\\Workflow};
  \node[actor, right of=n8n] (api) {Football\\API};
  
  % Flows
  \draw[arrow] (fe) -- node[above, font=\tiny] {GET /teams} (be);
  \draw[arrow] (be) -- node[above, font=\tiny] {Webhook trigger} (n8n);
  \draw[arrow] (n8n) -- node[above, font=\tiny] {HTTP request} (api);
  \draw[arrow, bend right=30] (api) -- node[below, font=\tiny] {JSON response} (n8n);
  \draw[arrow] (n8n) -- node[below, font=\tiny] {POST /api/v1} (be);
  \draw[arrow, bend right=30] (be) -- node[below, font=\tiny] {Data update} (fe);
\end{tikzpicture}%
}

%% DIAGRAM 7: LLM Prompt Engineering Pipeline
\newcommand{\DiagramPromptPipeline}{%
\begin{tikzpicture}[node distance=2cm, auto]
  \tikzset{input/.style={rectangle, draw, fill=orange!15, text width=1.5cm, text centered, minimum height=0.8cm, font=\small}}
  \tikzset{process/.style={rectangle, draw, fill=blue!15, text width=1.5cm, text centered, minimum height=0.8cm, font=\small}}
  
  % Inputs
  \node[input] (event) {Event\\Type};
  \node[input, below of=event] (facts) {Facts\\(Player, Time)};
  \node[input, below of=facts] (context) {Match\\Context};
  \node[input, below of=context] (style) {Few-Shot\\Examples};
  
  % Process
  \node[process, right of=facts, xshift=2cm] (prompt) {Build\\Prompt};
  
  % Output
  \node[process, right of=prompt, xshift=2cm] (llm) {LLM\\Call};
  \node[process, right of=llm] (output) {Ticker\\Text};
  
  % Connections
  \draw[arrow] (event) -- (prompt);
  \draw[arrow] (facts) -- (prompt);
  \draw[arrow] (context) -- (prompt);
  \draw[arrow] (style) -- (prompt);
  \draw[arrow] (prompt) -- (llm);
  \draw[arrow] (llm) -- (output);
\end{tikzpicture}%
}

%% DIAGRAM 8: Match Event Processing (Simplified Sequence)
\newcommand{\DiagramEventProcessing}{%
\begin{tikzpicture}[node distance=2.5cm, auto]
  \tikzset{stepbox/.style={rectangle, draw, fill=purple!15, text width=2.2cm, text centered, minimum height=0.9cm, font=\footnotesize}}
  
  \node[stepbox] (import) {1. Event\\Import};
  \node[stepbox, right of=import] (check) {2. Dedup\\Check};
  \node[stepbox, right of=check] (enrich) {3. Enrich\\Data};
  \node[stepbox, right of=enrich] (gen) {4. LLM\\Generate};
  \node[stepbox, below of=gen] (store) {5. Store\\Ticker};
  
  \draw[arrow] (import) -- (check);
  \draw[arrow] (check) -- (enrich);
  \draw[arrow] (enrich) -- (gen);
  \draw[arrow] (gen) -- (store);
  \draw[arrow, bend right=45] (store) -- node[right, font=\tiny] {broadcast} (import);
\end{tikzpicture}%
}

%% DIAGRAM 9: Ticker Generation Flow (Sequence-style, Kapitel 5)
\newcommand{\DiagramTickerGenerationFlow}{%
\begin{tikzpicture}[x=2.8cm, y=-1.1cm]
  \tikzset{
    actor/.style={rectangle, draw, fill=orange!15, text width=1.8cm, text centered,
                  minimum height=0.65cm, font=\footnotesize},
    msg/.style={->, thick, >=stealth, font=\tiny},
    retmsg/.style={->, thick, dashed, >=stealth, font=\tiny},
    altbox/.style={draw=blue!40, fill=blue!5, rounded corners=2pt},
  }
  % Participants (top row)
  \foreach \x/\lbl in {0/n8n,1/FastAPI,2/PostgreSQL,3/LLM,4/Frontend}{
    \node[actor] at (\x,0) {\lbl};
    \draw[gray!50, thin] (\x,0.35) -- (\x,9.5);
  }
  % Messages
  \draw[msg]    (0,1)   -- node[above]{POST /events}         (1,1);
  \draw[msg]    (1,1.8) -- node[above]{INSERT events}        (2,1.8);
  \draw[msg]    (0,2.6) -- node[above]{POST /ticker/generate/\{id\}} (1,2.6);
  \draw[msg]    (1,3.4) -- node[above]{SELECT (Dedup-Check)} (2,3.4);
  % Alt box: Neues Ereignis
  \draw[altbox] (0.05,3.9) rectangle (3.95,7.6);
  \node[font=\tiny, text=blue!70, anchor=north west] at (0.05,3.9) {[Neues Ereignis]};
  \draw[msg]    (1,4.4) -- node[above]{SELECT match + teams} (2,4.4);
  \draw[msg]    (1,5.1) -- node[above]{SELECT style\_refs}   (2,5.1);
  \draw[msg]    (1,5.8) -- node[above]{Prompt (Fakten+Kontext+Few-Shot)} (3,5.8);
  \draw[retmsg] (3,6.5) -- node[above]{Generierter Text}     (1,6.5);
  \draw[msg]    (1,7.2) -- node[above]{INSERT ticker\_entry (draft|published)} (2,7.2);
  \draw[retmsg] (1,7.9) -- node[above]{201 Created}          (0,7.9);
  % Polling
  \draw[msg]    (4,8.6) -- node[above]{GET /ticker/\{id\} (Polling 5 s)} (1,8.6);
  \draw[retmsg] (1,9.3) -- node[above]{TickerEntry[]}        (4,9.3);
\end{tikzpicture}%
}

%% DIAGRAM 10: React Frontend Component Tree (Kapitel 5)
\newcommand{\DiagramFrontendComponentTree}{%
\begin{tikzpicture}[
  comp/.style={rectangle, draw, fill=blue!10, rounded corners, text centered,
               font=\small, minimum height=0.6cm, text width=2.1cm},
  sub/.style={rectangle, draw, fill=blue!5, rounded corners, text centered,
              font=\tiny, minimum height=0.5cm, text width=1.7cm},
  ctx/.style={rectangle, draw=orange!60, fill=orange!10, rounded corners, text centered,
              font=\tiny, minimum height=0.5cm, text width=2cm},
  arr/.style={->, thick, >=stealth},
  ctxarr/.style={->, dashed, orange!70, >=stealth},
]
  % Root
  \node[comp] (app) at (5,0) {App};
  \node[comp] (lt)  at (5,-1.4) {LiveTicker};
  \draw[arr] (app) -- (lt);

  % Context annotation
  \node[ctx]  (ctx) at (9,-1.4) {3 Contexts:\\Mode / Data / Actions};
  \draw[ctxarr] (ctx.west) -- (lt.east);

  % Three panels
  \node[comp] (lp) at (1.5,-3) {LeftPanel};
  \node[comp] (cp) at (5,-3)   {CenterPanel};
  \node[comp] (rp) at (8.5,-3) {RightPanel};
  \draw[arr] (lt) -- (lp);
  \draw[arr] (lt) -- (cp);
  \draw[arr] (lt) -- (rp);

  % CenterPanel children
  \node[sub] (ec) at (3.2,-4.8) {EventCard};
  \node[sub] (ee) at (5,-4.8)   {EntryEditor};
  \node[sub] (ai) at (6.8,-4.8) {AIDraft};
  \draw[arr] (cp) -- (ec);
  \draw[arr] (cp) -- (ee);
  \draw[arr] (cp) -- (ai);

  % Context callbacks
  \draw[ctxarr, bend right=20] (ec.north) to node[left,font=\tiny]{onGenerate} (ctx.south);
  \draw[ctxarr, bend left=10]  (ai.north) to node[right,font=\tiny]{onPublish} (ctx.south);

  % LeftPanel child
  \node[sub] (pe) at (1.5,-4.8) {PublishedEntry};
  \draw[arr] (lp) -- (pe);

  % RightPanel children
  \node[sub] (coll) at (8.5,-4.8) {Collapsible\\StatRow etc.};
  \draw[arr] (rp) -- (coll);
\end{tikzpicture}%
}
